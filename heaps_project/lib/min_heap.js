class MinHeap {
    constructor(array = null) {
       this.array = [null];
    }

    getParent(idx) {
      return Math.floor(idx / 2);
    }

    getLeftChild(idx) {
        
    }

    getRightChild(idx) {
        
    }

    insert(val) {
      
    }

    siftUp(idx) {
      
    }
    deleteMax() {
       
    }

    siftDown(idx) {
       
    }
}

module.exports = {
    MinHeap
};